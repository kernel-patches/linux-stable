# Stub denylist for the BPF-CI-on-stable demo.
#
# This file exists only to demonstrate that the sync job overlays ci/ onto every
# version branch, and that the CI workflow (test.yml) can read it from the
# branch. The real test configuration -- denylists, ci/diffs, the kernel build
# and selftest steps -- is owned by Shung-Hsi and goes here on `main`.
